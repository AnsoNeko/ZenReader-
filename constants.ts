import { AppSettings } from "./types";

export const DEFAULT_SETTINGS: AppSettings = {
  theme: 'light',
  fontSize: 18,
  lineHeight: 1.6,
  fontFamily: 'serif',
};

export const DB_NAME = 'ZenReaderDB';
export const STORE_NAME = 'books';
